      border-radius: 999px;
      background: rgba(0, 0, 0, 0.34);
      color: #fff;
      font: inherit;
      font-size: 14px;
      letter-spacing: 0;
      cursor: pointer;
      backdrop-filter: blur(12px);
    }

    #CinematicIntro-{{ section.id }} .cinematic-intro__skip:hover,
    #CinematicIntro-{{ section.id }} .cinematic-intro__skip:focus-visible {
      background: rgba(255, 255, 255, 0.14);
      outline: none;
    }

    @keyframes cinematic-logo-in {
      to {
        opacity: 1;
        transform: translateY(0) scale(1);
      }
    }

    @media (prefers-reduced-motion: reduce) {
      #CinematicIntro-{{ section.id }} {
        display: none;
      }
    }
  </style>

  <script>
    (() => {
      const intro = document.getElementById('CinematicIntro-{{ section.id }}');
      if (!intro) return;

      const video = intro.querySelector('video');
      const skip = intro.querySelector('button');
      const sessionKey = intro.dataset.sessionKey;
      const playOnce = intro.dataset.playOnce === 'true';

      const hideIntro = () => {
        intro.classList.add('is-hidden');
        if (playOnce) sessionStorage.setItem(sessionKey, 'true');
        window.setTimeout(() => intro.remove(), 1000);
      };

      if (playOnce && sessionStorage.getItem(sessionKey) === 'true') {
        intro.remove();
        return;
      }

      skip?.addEventListener('click', hideIntro);
      video?.addEventListener('ended', hideIntro);

      window.setTimeout(hideIntro, {{ section.settings.max_duration | times: 1000 }});
    })();
  </script>
{% endif %}

{% schema %}
{
  "name": "Cinematic intro",
  "settings": [
    {
      "type": "checkbox",
      "id": "enabled",
      "label": "Activer l'intro",
      "default": true
    },
    {
      "type": "video",
      "id": "video",
      "label": "Video d'intro"
    },
    {
      "type": "image_picker",
      "id": "poster",
      "label": "Image de chargement"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo au centre"
    },
    {
      "type": "checkbox",
      "id": "play_once",
      "label": "Afficher une seule fois par session",
      "default": true
    },
    {
      "type": "range",
      "id": "max_duration",
      "label": "Duree maximum",
      "min": 2,
      "max": 12,
      "step": 1,
      "unit": "s",
      "default": 6
    }
  ],
  "presets": [
    {
      "name": "Cinematic intro"
    }
  ]
}
{% endschema %}

      border: 0;
      border-radius: 14px;
      background: #fff;
      color: #000;
      font: inherit;
      font-size: 18px;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 18px 44px rgba(0, 0, 0, 0.35);
    }

    @media (prefers-reduced-motion: reduce) {
      #CinematicIntro-{{ section.id }} {
        display: none;
      }
    #CinematicIntro-{{ section.id }} .cinematic-intro__enter:hover,
    #CinematicIntro-{{ section.id }} .cinematic-intro__enter:focus-visible {
      transform: translateY(-1px);
      outline: 2px solid rgba(255, 255, 255, 0.72);
      outline-offset: 4px;
    }
  </style>

      const intro = document.getElementById('CinematicIntro-{{ section.id }}');
      if (!intro) return;

      const video = intro.querySelector('video');
      const skip = intro.querySelector('button');
      const sessionKey = intro.dataset.sessionKey;
      const playOnce = intro.dataset.playOnce === 'true';

      const hideIntro = () => {
        intro.classList.add('is-hidden');
        if (playOnce) sessionStorage.setItem(sessionKey, 'true');
        window.setTimeout(() => intro.remove(), 1000);
      };

      if (playOnce && sessionStorage.getItem(sessionKey) === 'true') {
        intro.remove();
        return;
      }

      skip?.addEventListener('click', hideIntro);
      video?.addEventListener('ended', hideIntro);
      document.documentElement.classList.add('cinematic-intro-active');

      window.setTimeout(hideIntro, {{ section.settings.max_duration | times: 1000 }});
      if (intro.parentElement !== document.body) {
        document.body.appendChild(intro);
      }

      const hideIntro = () => {
        intro.classList.add('is-hidden');
        document.documentElement.classList.remove('cinematic-intro-active');
        if (playOnce) sessionStorage.setItem(sessionKey, 'true');
        window.setTimeout(() => intro.remove(), 850);
      };

      intro.querySelector('.cinematic-intro__enter')?.addEventListener('click', hideIntro);
    })();
  </script>
{% endif %}
    },
    {
      "type": "image_picker",
      "id": "poster",
      "label": "Image de chargement"
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo au centre"
      "type": "text",
      "id": "heading",
      "label": "Titre",
      "default": "Bienvenue"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Sous-titre",
      "default": "Decouvrez notre collection"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Texte du bouton",
      "default": "Entrer"
    },
    {
      "type": "checkbox",
      "id": "play_once",
      "label": "Afficher une seule fois par session",
      "default": true
    },
    {
      "type": "range",
      "id": "max_duration",
      "label": "Duree maximum",
      "min": 2,
      "max": 12,
      "step": 1,
      "unit": "s",
      "default": 6
    }
  ],
  "presets": [

    {% if template.name == 'index' and section.settings.video != blank %}
<div id="cinematicIntro">
  <video autoplay muted playsinline loop>
    {% for source in section.settings.video.sources %}
      <source src="{{ source.url }}" type="{{ source.mime_type }}">
    {% endfor %}
  </video>

  <div class="intro-content">
    <h1>{{ section.settings.title }}</h1>
    <p>{{ section.settings.subtitle }}</p>
    <button id="enterSite">Entrer</button>
  </div>
</div>

<style>
  html.intro-lock,
  html.intro-lock body {
    overflow: hidden !important;
    height: 100% !important;
  }

  #cinematicIntro {
    position: fixed !important;
    inset: 0 !important;
    width: 100vw !important;
    height: 100vh !important;
    z-index: 2147483647 !important;
    background: #000 !important;
    overflow: hidden !important;
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
  }

  #cinematicIntro video {
    position: absolute !important;
    inset: 0 !important;
    width: 100% !important;
    height: 100% !important;
    object-fit: cover !important;
  }

  .intro-content {
    position: relative !important;
    z-index: 2 !important;
    text-align: center !important;
    color: white !important;
  }

  .intro-content h1 {
    color: white !important;
    font-size: clamp(48px, 8vw, 96px) !important;
    margin: 0 0 20px !important;
  }

  .intro-content p {
    color: white !important;
    font-size: 22px !important;
    margin: 0 0 40px !important;
  }

  #enterSite {
    background: white !important;
    color: black !important;
    border: 0 !important;
    border-radius: 14px !important;
    padding: 18px 50px !important;
    font-size: 18px !important;
    font-weight: 700 !important;
    cursor: pointer !important;
  }

  #cinematicIntro.hide {
    opacity: 0 !important;
    pointer-events: none !important;
    transition: opacity .7s ease !important;
  }
</style>

<script>
  document.documentElement.classList.add('intro-lock');

  const intro = document.getElementById('cinematicIntro');
  const btn = document.getElementById('enterSite');

  btn.addEventListener('click', function () {
    intro.classList.add('hide');
    document.documentElement.classList.remove('intro-lock');

    setTimeout(function () {
      intro.remove();
    }, 700);
  });
</script>
{% endif %}

{% schema %}
{
  "name": "Cinematic intro",
  "settings": [
    {
      "type": "video",
      "id": "video",
      "label": "Video d'intro"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Titre",
      "default": "Bienvenue"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Sous-titre",
      "default": "Decouvrez notre collection"
    }
  ],
  "presets": [
    {
      "name": "Cinematic intro"
    }
  ]
}
{% endschema %}